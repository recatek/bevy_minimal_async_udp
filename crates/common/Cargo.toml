[package]
name = "common"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.7"
flume = "0.10.13"       # Used for threaded message passing
async-net = "1.6.1"     # Used for UDP provider
futures-lite = "1.12.0" # Used to block on UdpSocket bind
